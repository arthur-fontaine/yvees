{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": ["QR_DECODE_SERVICE_PORT"],
  "tasks": {
    "build": {},
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "start": {
      "dependsOn": ["^build"]
    }
  }
}
